
import { pgSchema } from 'drizzle-orm/pg-core';

export const planFeaturesSchema = pgSchema('plan_features');

// Define columns and types as per requirements
// Example:
// planFeaturesSchema.addColumn('plan', { type: 'string' });
// planFeaturesSchema.addColumn('feature', { type: 'string' });
// planFeaturesSchema.addColumn('limit', { type: 'number' });
