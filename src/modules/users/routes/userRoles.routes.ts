import { Router } from 'express';
// Implement endpoints for assigning/removing roles to users
const router = Router();

router.post('/assign', (req, res) => {});
router.get('/:userId', (req, res) => {});
router.delete('/remove', (req, res) => {});

export default router;
